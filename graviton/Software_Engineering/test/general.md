# Test Questions
**Company:** Graviton
**Profile:** Software Engineering

Pen and paper test.

- 3 coding questions (graph algorithms, DSU, multiset / segment tree).
- MCQs on C++ theory, networks and OS.

**Last coding question (most marks, DSU + BFS)**
Edges are given for a graph. Wherever one end of an edge is given as 0, it means it
is a particular unspecified node that you don't know about; other nodes are from 1
to n. You have to assume that the unspecified node is i, and if we can then reach N
from 1, store the distance in `result[i]`, else store -1 in `result[i]`. Return
`result`.

(Eshika - rejected. Sujal - rejected.)
