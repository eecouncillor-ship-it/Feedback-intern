# Interview Questions
**Company:** GlobalFoundries 
**Profile:** Embedded

A lot of architecture stuff pipelines data dependencies operand forwarding,basic C data like heap allocator implementing a custom allocator, basic dsa linked lists,heapify an array, memory management in C given a struct show each var and basic pointer math