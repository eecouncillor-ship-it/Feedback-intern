# Interview Questions
**Company:** Stripe
**Profile:** Software Engineering

**Result:** Rejected. (Eshika was also rejected.)

**Interview 1**
- Given a dataset represented as a CSV, parse rows based on given conditions
  (e.g. if any entry in a row is empty or has length > x or < y, print that row as
  invalid, else valid).
- Two follow-up questions using the same setup as the first question but with more
  constraints.

Do not code in C++ for Stripe - prefer Python. This is very important: my
interviewer actually said that C++ code gets dirty and is not very maintainable.
They really want reproducible and clean code.
